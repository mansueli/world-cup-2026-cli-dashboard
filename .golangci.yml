version: "2"

run:
  tests: false

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-rules:
    - path: cmd/flaggen/
      linters:
        - errcheck
        - gosec

linters:
  enable:
    - bodyclose
    - errcheck
    - ineffassign
    - gosec
    - govet
    - nilerr
    - predeclared
    - staticcheck
    - unconvert
    - unused
    - whitespace

formatters:
  enable:
    - goimports
